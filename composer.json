{
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },

    "scripts": {
        "test": "vendor/bin/phpunit src/tests",
        "test:coverage": "vendor/bin/phpunit --coverage-html coverage-result/ src/tests/"
    }
}